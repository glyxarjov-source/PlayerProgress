import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.playerprogress.app',
  appName: 'PlayerProgress',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
